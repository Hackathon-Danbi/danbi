# iOS 시뮬레이터/웹/Android 에뮬레이터는 환경에 맞는 백엔드 주소를 사용하세요.
# 실제 휴대폰에서는 localhost 대신 개발 PC의 같은 Wi-Fi(LAN) IP를 입력합니다.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080

# API 요청 타임아웃(ms). 미설정 시 5000. 초과하면 로컬/목 데이터로 폴백합니다.
# EXPO_PUBLIC_API_TIMEOUT_MS=5000
